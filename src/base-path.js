const base = import.meta.env.BASE_URL;

document.documentElement.style.setProperty(
  "--contacts-bg",
  `url('${base}assets/contacts-bg.png')`,
);
document.documentElement.style.setProperty(
  "--gifka",
  `url('${base}assets/gifka.gif')`,
);
